# Playwright e2e artifacts (on-demand run output)
/test-results/
/playwright-report/
/blob-report/
/.playwright/
/playwright/.cache/

# vue-tsc's incremental build stamp — a local artifact, never a deliverable.
tsconfig.tsbuildinfo
